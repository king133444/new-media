# 端口
PORT=3000
NODE_ENV=production

# MySQL 连接（注意 @ 需 URL 编码为 %40）
# 假设: 用户 root，密码 Jj123456@，库名 new_media
DATABASE_URL=mysql://root:Jj123456%40@localhost:3306/new_media

# JWT
JWT_SECRET=media_secret_key
JWT_EXPIRES_IN=7d

# 上传
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# 如需限制跨域来源可改为逗号分隔，若用同源反代可忽略
CORS_ORIGINS=http://8.130.10.227